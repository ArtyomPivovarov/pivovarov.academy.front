/**
 * Mock mode configuration utilities
 */

export const MOCK_CONFIG = {
  // Auto-login on app start
  autoLogin: true,
  
  // Log mock mode actions to console
  enableLogging: true,
  
  // Default mock user email
  defaultUserEmail: 'demo@pivovarov.academy',
} as const
